**任务: APP_8920A2_DAC_ADC_IIS.rar 关键函数重写**

1. 进以下述4类重写函数操作,分别对应DAC/ADC/IIS发送/IIS接收.
2. TEST_PCM2DAC (DAC推数据): 程序中DAC SRCIN采样率是8K, 请把采样率改16K(dac_spr_set(SPR_16000))并推出500HZ, 1KHZ, 2KHZ 三种正弦波 (各用一个正弦波数组实现,并上传对应正弦波数组).DAC输出需要用示波器及audacity录音确认频率正常,声音无断续
3. TEST_AUX_ADC2DAC (AUX ADC采样后推DAC): AUX ADC 中断推DAC函数 auxadc_pcm_to_dac 在库中,该函数需要在外面重写实现. AUX输入为正弦波时,DAC输出需要用示波器及audacity录音确认频率正常,声音无断续
4. TEST_AUX_ADC2IISSRCTX (AUX ADC采样后从IIS推出): iis_master_srctx_init()在库中,需要在外面重写,该函数实现的是IIS_MASTER_SRCTX功能. 可用逻辑分析仪抓取IIS的BCLK,LRC,DO 三个IO口,查看是否有数据输出. 也可以对接到另一颗芯片(配置TEST_IISRX2DAC)功能上,通过DAC听该IIS发送数据是否正常.
5. TEST_IISRX2DAC (IIS接收数据并从DAC中推出): iis_slave_ram_rx_2_dac()函数在库中,需要重写. 可以用另外一颗芯片,配置成 TEST_AUX_ADC2IISSRCTX,该芯片AUX→ADC→IIS发送出来的音频数据 通过 iis_slave_ram_rx_2_dac 接收到后,从DAC中推出来,验证接收是否正常.

**任务: SDDAC驱动实战**

1. 建立 sddac_test 目录
2. 阅读芯片寄存器手册，配置SDDAC
3. 使用上面(Audacity + HxD)生成的C语言数组, 推DAC并用示波器查看DAC输出是否为正弦波.
4. 理解采样率和正弦波频率的关系, 使用一个周期的正弦波数组,不断推DAC并输出500HZ, 1KHz, 2KH三种频率正弦波. 可以使用示波器或Audacity录音的频谱分析功能查看正弦波频率是否正确.
5. 理解DAC数字音量和模拟音量关系, DAC输出1KHz正弦波时.用两个按键分别实现16级音量的调小和调大DAC输出. 使用两种方式调音量(1.固定数字音量,只调模拟音量. 2.固定模拟音量,只调数字音量).
6. 理解调音DB数与DAC输出峰峰值的关系,如固定数字音量,只调模拟音量时, 模拟音量从0DB调低到-6DB时,示波器查看DAC峰峰值下降了多少?

**SDADC驱动实战**

1. 建立 sdadc_test 目录
2. 阅读芯片寄存器手册，配置SDADC
3. 尝试SDADC数据采样
4. 熟悉并配值SDADC的使用
5. 尝试将SDADC采样的数据通过SDDDAC进行输出

**IIS驱动实战**

1. 建立 iis_test 目录
2. 尝试从ADC采集数据，并配置DAC通过IIS输出声音
3. 尝试从IIS获取音频数据并从DAC输出声音
4. 整合以上两套系统，实现 ADC→IIS OUT→IIS IN→DAC这一链路

